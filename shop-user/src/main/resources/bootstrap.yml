server:
  port: 8061

spring:
  application:
    name: shop-user
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #Nacos配置中心的地址
        file-extension: yaml #配置文件的后缀名
  profiles:
    active: dev